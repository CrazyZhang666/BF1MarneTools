<ui:EdgeWindow
    x:Class="BF1MarneTools.Windows.AccountWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:root="clr-namespace:BF1MarneTools"
    xmlns:ui="http://wpf.microsoft.com/ui/2026/xaml"
    x:Name="Window_Account"
    Title="战地1马恩工具箱"
    Width="500"
    Height="260"
    MinWidth="500"
    MinHeight="260"
    Closing="Window_Account_Closing"
    ContentRendered="Window_Account_ContentRendered"
    DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
    Icon="{StaticResource DefaultIconPath}"
    Loaded="Window_Account_Loaded"
    ResizeMode="NoResize"
    Style="{StaticResource EdgeWindowStyle}"
    WindowStartupLocation="CenterOwner"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel>
            <ui:IconHeader Title="EA账号邮箱" Margin="5,5,5,0" />
            <ui:TextBoxHint
                x:Name="TextBoxHint_Email"
                Hint="输入拥有战地1游戏的EA账号的邮箱"
                Icon="&#xE715;" />

            <ui:IconHeader Title="EA账号密码" Margin="5,0,5,0" />
            <ui:TextBoxHint
                x:Name="TextBoxHint_Password"
                Hint="输入拥有战地1游戏的EA账号的密码"
                Icon="&#xE8D7;" />

            <TextBlock
                Margin="7,5,7,5"
                Foreground="{StaticResource TextSecondColor}"
                TextWrapping="Wrap">
                提示：请输入任意拥有战地1游戏的EA账号邮箱和密码，即使这个账号被协议6封禁，或者被EAAC或FF封禁。只需要账号拥有战地1游戏即可，无论游戏版本，主要是为了让EA下发战地1的「Denuvo加密」许可证，否则无法启动游戏。
            </TextBlock>
        </StackPanel>

        <StackPanel
            Grid.Row="1"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <ui:IconButton
                Margin="0,5,5,5"
                Command="{Binding EnterAccountCommand}"
                Content="确认账号"
                Icon="&#xF78C;" />
        </StackPanel>
    </Grid>

</ui:EdgeWindow>
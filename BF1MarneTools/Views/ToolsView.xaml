<UserControl
    x:Class="BF1MarneTools.Views.ToolsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:res="clr-namespace:BF1MarneTools.Resources"
    xmlns:root="clr-namespace:BF1MarneTools"
    xmlns:ui="http://wpf.microsoft.com/ui/2025/xaml"
    d:DesignHeight="100"
    d:DesignWidth="800"
    DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
    mc:Ignorable="d">

    <DockPanel Margin="3">
        <!--  按钮功能  -->
        <WrapPanel
            VerticalAlignment="Center"
            DockPanel.Dock="Right"
            Visibility="{Binding Path=(root:Globals.IsUseServer), Converter={StaticResource EnabledToVisibility2Converter}}">
            <CheckBox
                Margin="2.5"
                Content="{x:Static res:Lang.ToolsView_ShowFPS}"
                IsChecked="{Binding Path=(root:Globals.IsDrawFps)}" />
        </WrapPanel>

        <!--  开关功能  -->
        <WrapPanel VerticalAlignment="Center">
            <ui:IconButton
                Margin="2.5"
                Command="{Binding ClearModDataCommand}"
                Content="{x:Static res:Lang.ToolsView_ClearModData}"
                Icon="&#xE74D;" />
            <ui:IconButton
                Margin="2.5"
                Command="{Binding KillBf1ProcessCommand}"
                Content="{x:Static res:Lang.ToolsView_KillBF1Process}"
                Icon="&#xF78A;" />
            <Border
                Width="1"
                Margin="7"
                Background="Gray" />
            <!--  //////  -->
            <ui:IconButton
                Margin="2.5"
                Command="{Binding EditServerSettingsCommand}"
                Content="{x:Static res:Lang.ToolsView_EditServerSettings}"
                Icon="&#xE74C;" />
            <ui:IconButton
                Margin="2.5"
                Command="{Binding EditMapListCommand}"
                Content="{x:Static res:Lang.ToolsView_EditMapList}"
                Icon="&#xE826;" />
            <Border
                Width="1"
                Margin="7"
                Background="Gray" />
            <!--  //////  -->
            <ui:IconButton
                Margin="2.5"
                Command="{Binding OpenIPOverrideWebCommand}"
                Content="{x:Static res:Lang.ToolsView_IPRedirect}"
                Icon="&#xE909;" />
        </WrapPanel>
    </DockPanel>

</UserControl>
<UserControl
    x:Class="BF1MarneTools.Views.LogView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:res="clr-namespace:BF1MarneTools.Resources"
    xmlns:root="clr-namespace:BF1MarneTools"
    d:DesignHeight="400"
    d:DesignWidth="800"
    DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
    mc:Ignorable="d">

    <ListView
        x:Name="ListView_Logger"
        Margin="5,0,5,5"
        ItemsSource="{Binding ObserColl_LogInfos}">
        <ListView.View>
            <GridView ColumnHeaderContainerStyle="{StaticResource GridViewColumnHeaderStyle}">
                <GridViewColumn
                    Width="60"
                    DisplayMemberBinding="{Binding Time}"
                    Header="{x:Static res:Lang.LogView_Time}" />
                <GridViewColumn
                    Width="55"
                    DisplayMemberBinding="{Binding Level}"
                    Header="{x:Static res:Lang.LogView_Level}" />
                <GridViewColumn
                    Width="700"
                    DisplayMemberBinding="{Binding Message}"
                    Header="{x:Static res:Lang.LogView_Message}" />
            </GridView>
        </ListView.View>
    </ListView>

</UserControl>
